</p><strong>by: <%= User.find(comment.user_id).email.split('@').first %></strong><p>
<p><%= comment.body %></p>
<p><small><%= time_ago_in_words(comment.created_at) %> ago</small></p>

<p><%= link_to 'Delete', [comment.event,comment], method: :delete, data: { confirm: 'Are you sure?' } , class: 'btn btn-danger' %></p>